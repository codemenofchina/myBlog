<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiang.blog.dao.UserDao">

    <select id="queryAll" resultType="User">
        SELECT *
        FROM user
        ORDER BY id
        limit #{offset},#{limit}
    </select>

    <insert id="addUser" useGeneratedKeys="true" keyProperty="id">
        INSERT
          INTO user
          (user_account, password, user_name, user_type, user_describe)
        VALUES
        (#{userAccount}, #{password}, #{userName}, #{userType}, #{userDescribe})
    </insert>
</mapper>